Este ejercicio no es para programar. Lean el codigo y escriban una explicación de que hace.

linea = " mara ; programacion ; 8 "

partes = linea.split(";")
nombre = partes[0].strip().capitalize()
materia = partes[1].strip().capitalize()
nota_texto = partes[2].strip()

if nota_texto.isnumeric():
    nota = int(nota_texto)
    print(f"{nombre} cursa {materia} y obtuvo {nota}")
else:
    print("La nota no es valida")
Para orientar la explicacion:

A)¿Que queda guardado en partes?
B)¿Por que se usa strip antes de capitalize?
C)¿Que dato se esta validando antes de convertirlo?
D)¿Que imprimiria el programa si en lugar de 8 viniera ocho?

A) En este codigo lo que queda guardado en (partes) es una lista divida por comas.
B) Luego se utiliza strip() y capatilize(), en primer lugar obviamente se debe utilizar, strip() debido a que esto elimina--
los espacios del principio y el final, por otro lado si quisieramos utilizar capitalize() antes que Strip(), esto nos daria error--
ya que esta funcion lo que hace es poner en mayuscula el primer caracter y como el primer caracter es un espacio romperia todo.
C) El Dato que se esta validando es que nota_texto sea si o si, numeros. --> isnumeric() solo valida que los datos sean numeros, como es ocho nos devuelve True por ende se cumple lo siguiente.
D) Como tenemos una cadena de caracteres --> "ocho" el codigo se guiara por el else, por ende nos imprimira "La nota no es valida"